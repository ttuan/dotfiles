-- local lspconfig = require("lspconfig")

return {
  {
    "neovim/nvim-lspconfig",
    opts = {
      -- servers = {
      --   ruby_lsp = {
      --     cmd = { "ruby-lsp" },
      --     init_options = {
      --       formatter = "auto",
      --     },
      --   },
      -- },
    },
  },
}
